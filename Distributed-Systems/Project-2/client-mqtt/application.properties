# MQTT Configuration

mqtt.broker.url=tcp://localhost:1883
mqtt.client.id=client-publisher

# At least once delivery
mqtt.qos=1
mqtt.topic=sensors/

# ActiveMQ MQTT credentials
mqtt.username=admin
mqtt.password=admin